<UserControl x:Class="BackEndView.Views.KuchenverwaltungView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BackEndView.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="11*"/>
            <RowDefinition Height="19*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical">
        <Label Content="Neuen Kuchen hinzufügen:" FontStyle="Oblique"/>
            <StackPanel Orientation="Horizontal">
                <Label Content="Name:"/>
                <TextBox Width="150" Height="20"/>
            </StackPanel>
        </StackPanel>
        


        <DataGrid Margin="10" Grid.Row="1"/>
        <!-- DataGrid.Colums mit Button für Änderungen-->

    </Grid>
</UserControl>
